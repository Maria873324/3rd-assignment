<!DOCTYPE html>
<html>
<head>
  <title>Your Image Collection</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>📸 Image Gallery</h1>
  </header>
  <div class="container">
    <a href="/upload">➕ Upload New Image</a>
    <div>
      <% if (images.length > 0) { %>
        <% images.forEach(image => { %>
          <img src="<%= image %>" alt="uploded image">
        <% }) %>
      <% } else { %>
        <p>No images uploaded yet.</p>
      <% } %>
    </div>
  </div>
</body>
</html>